
# Open docker app (or simply the deamon) 
docker desktop start
# open -a Docker; until docker info 2> /dev/null; do sleep 1; done # Robust way to open it up 

# To stop it 
# docker desktop stop

# To build the app 
# NOTE : This will take some time (400s)
cd ~/Github_proj/lmmjvR
docker build -t hr_sum --platform linux/amd64 .

# Run the app 
docker run -p 8080:3838 hr_sum

# Then open browser
http://localhost:8080
